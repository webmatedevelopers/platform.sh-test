{% extends 'base.html.twig' %}

{% block title %}Hello IndexController!{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="example-wrapper">
        <h1>Hello! ✅</h1>

        Your cars:
        <ul>
            {% for car in cars %}
                <li>{{ car.name }} {% if car.color %}<small style="color: {{ car.color }};">{{ car.color }}</small>{% endif %}</li>
            {% endfor %}
        </ul>
    </div>
{% endblock %}
